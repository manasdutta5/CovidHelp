<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covidhelp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/covidapp3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/covidapp3"
        android:supportsRtl="true"
        android:theme="@style/Theme.CovidHelp">
        <activity android:name=".HelpActivity"></activity>
        <activity android:name=".VaccineActivity" />
        <activity
            android:name=".Interact"
            android:label="Connect with a volunteer." />
        <activity
            android:name=".VolunteerList"
            android:label="@string/category_volunteerList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>